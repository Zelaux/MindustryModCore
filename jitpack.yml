jdk:
  - openjdk16
before_install:
  - wget https://github.com/sormuras/bach/raw/master/install-jdk.sh
  - source install-jdk.sh --feature 16
  - jshell --version
install:
  - chmod +x gradlew
  - ./gradlew publishToMavenLocal -DjvmArgs--add-opens=jdk.compiler/com.sun.tools.javac.code=ALL-UNNAMED